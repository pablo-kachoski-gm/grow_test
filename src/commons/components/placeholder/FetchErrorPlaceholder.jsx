const FetchErrorPlaceholder = () => {
  const errorMessage =
    "There was an error getting the data. Please contact the administrator";
  return <div>{errorMessage}</div>;
};

export default FetchErrorPlaceholder;
